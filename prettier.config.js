module.exports = {
  tabWidth: 2, // Таб размер
  semi: true, // точка с запятой после каждой строки
  // quoteProps: "preserve" // Коректно не работает!
  trailingComma: 'es5', // с запятыми в конце.
  bracketSpacing: true, // пробелы между скобками
  jsxBracketSameLine: false, // переносить > на следующую строчку при большом количестве атрубутов
  arrowParens: 'avoid', // в стрелочных функциях удалять скобки у 1 параметра
  printWidth: 80, //  длина строки не должна превышать 80 символов.
}
